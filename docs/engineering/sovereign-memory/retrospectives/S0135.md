# Curator Retrospective — S0135 / BUG-0017 (OpenCode pack LF / Linux slash commands)

```json
{
  "sprint_id": "S0135",
  "story_ids": ["BUG-0017"],
  "release_ref": "handoffs/releases/S0135-release-notes.md",
  "summary": "BUG-0017 shipped scoped .gitattributes LF rows + renormalize + guard_installer_publish OpenCode CR inventory + 6 test_bug0017_* markers + DQ6 upgrade recipe + before-tag guard:installer note; compose BUG-0008/US-0084/DEC-0120; no companion DEC; lifecycle closed through refresh-context.",
  "learnings": [
    "Scoped .opencode/** + template/.opencode/** eol=lf — reject repo-wide *.md eol=lf (D3/D8).",
    "Attributes + one-time renormalize + publish guard beats install-time EOL rewrite (DQ3).",
    "Kit fix alone does not heal already-installed CRLF trees — DQ6 upgrade --host opencode|both is required consumer path.",
    "npm ships template/.opencode only; active↔template parity tests still required for kit checkout.",
    "choco before-tag guard:installer remains release/ops enforcement — comment in chocolateyInstall.ps1 + runbook."
  ],
  "promoted_entry_ids": []
}
```

- **Bug**: BUG-0017 (OpenCode on Linux ignores its-magic slash commands — CRLF breaks YAML frontmatter)
- **Sprint**: S0135 (RELEASED — lifecycle terminal at refresh-context 2026-09-11T20:29:00Z)
- **Orchestrator run**: `auto-20260911-bug0017`
- **Verdict**: PASS (segment_closed=true; acceptance BUG-0017 [x]; 6/6 test_bug0017_*; guard:installer PASS; UAT 8/8; harness Pass:857/Fail:0 @ 2026-09-11T20:18:29Z)
- **Curator runtime proof**: `rp-auto-20260911-bug0017-refresh-context-curator-20260911T202900Z-BUG-0017`
- **Curator fresh_context_marker**: `cur-BUG0017-refresh-20260911T202900Z-fresh`

## Lifecycle summary

BUG-0017 fully closed: discovery → research (R-0118) → architecture (A*) → sprint-plan S0135 → execute → qa → verify-work → release → closure → sovereign-critic (closure) → refresh-context.

Final state: S0135 RELEASED; BUG-0017 DONE; portfolio 0 OPEN stories / 0 OPEN bugs.

## Drain state

`drain_terminated=true` (`no_open_stories`). Orchestrator owns sovereign-critic of refresh-context then `advance_sovereign_loop` — curator does not drain-advance.
