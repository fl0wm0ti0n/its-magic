# Curator Retrospective — S0148 / BUG-0023 (OpenCode CLI TUI listed `/auto` toasts OPENCODE_AUTO_TUI_DISPATCH_UNSUPPORTED)

```json
{
  "sprint_id": "S0148",
  "story_ids": ["BUG-0023"],
  "release_ref": "handoffs/releases/S0148-release-notes.md",
  "summary": "BUG-0023 shipped Axis A shared Rpc.define rpc.ts, orchestrator await ctx.rpc.register, TUI dispatchRunAutoLifecycle client.rpc(Defined) plus OpenCode.make fallback, invented POST removed, DISPATCH only when client/RPC truly absent, keep { id, tui } plus editor.add, upgrade overwrite, 8/8 test_bug0023_* plus bug0021/0020/0019/0018 compose; no companion DEC; no auto.md restore; live CLI TUI not probed; lifecycle closed through refresh-context.",
  "learnings": [
    "Listing `/auto` is not dispatch — host-true Rpc.define + client.rpc(Defined) / OpenCode.make is required to start runAutoLifecycle.",
    "Invented POST /rpc/{id}/{method} { input } is not the happy path; DISPATCH toast is residual only when client/RPC are truly absent.",
    "Keep { id, tui } listing and editor.add execute; do not restore STOP-only auto.md because DISPATCH fired.",
    "Explicit bug-target /auto run terminates at refresh-context — do not drain-advance to BUG-0022 or US-0141.",
    "Closure owns exclusive DONE flip + acceptance tick; refresh-context owns compaction/retrospective only — no backlog mutation."
  ],
  "promoted_entry_ids": []
}
```

- **Bug**: BUG-0023 (OpenCode CLI TUI listed `/auto` toasts OPENCODE_AUTO_TUI_DISPATCH_UNSUPPORTED)
- **Sprint**: S0148 (RELEASED — lifecycle terminal at refresh-context 2026-09-14T01:25:00Z)
- **Orchestrator run**: `auto-20260913-bug0023`
- **Verdict**: PASS (segment_closed=true; acceptance BUG-0023 [x]; 8/8 test_bug0023_*; bug0021 8/8; bug0020 8/8; bug0019 7/7; bug0018 6/6; UAT 10/10; scoped pytest 37/37)
- **Curator runtime proof**: `rp-auto-20260913-bug0023-refresh-context-curator-20260914T012500Z-BUG-0023`
- **Curator fresh_context_marker**: `cur-BUG0023-refresh-20260914T012500Z-fresh`

## Lifecycle summary

BUG-0023 fully closed: discovery → research (R-0137) → architecture (`# BUG-0023` / Axis A) → sprint-plan S0148 → execute → qa → verify-work → release → closure → sovereign-critic (closure) → **refresh-context**.

Final state: S0148 RELEASED; BUG-0023 DONE. Portfolio 8 OPEN stories (US-0141..US-0148) / 1 OPEN bug (BUG-0022). Honest residual: Axis A shipped (`Rpc.define` + `client.rpc(Defined)`); live CLI TUI not probed; no auto.md restore.

## Drain state

`drain_advance_action=not_applicable`. Explicit bug-target segment — orchestrator STOP after refresh-context. Do **not** select BUG-0022. Do **not** drain-advance to US-0141. Curator does not drain-advance. `native_chain_continuing=false`. `stop_reason=completed`. Sibling US-0141 (`auto-20260913-us0141` / S0149) continues separately toward `/closure`.
