# Release notes — 0.1.4

<!-- release_changelog_fingerprint: 0.1.4|BUG-0025 -->

> Per-version GitHub `-F` SOT (**US-0100**). Sprint-scoped evidence remains in `handoffs/releases/Sxxxx-release-notes.md` — do not overwrite unrelated version files.

## Work items

- **BUG-0025** — Packaged `its-magic` npm tarball now includes `scripts/standalone_runtime_install_lib.py`; fail-closed standalone bootstrap loader (`STANDALONE_BOOTSTRAP_FAILED`); kit `0.1.4` packaging twins; six `test_bug0025_*` markers. npm publish deferred to operator confirm (`RELEASE_PUBLISH_MODE=confirm`).

## Sprint evidence

- [`S0157`](handoffs/releases/S0157-release-notes.md)
