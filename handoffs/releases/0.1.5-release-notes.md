# Release notes - 0.1.5

<!-- release_changelog_fingerprint: 0.1.5|BUG-0026 -->

> Per-version GitHub `-F` source of truth. Sprint-scoped evidence remains in its existing release notes.

## Work items

- **BUG-0026** - Fix the published-kit standalone bootstrap by shipping `scripts/standalone-supported-kernel-range.json` and required installer peers. `its-magic --target . --mode upgrade --host both` now materializes `.its-magic/bin/itsm` without a false `KERNEL_CONTRACT_MISMATCH` caused by the intentionally omitted private root `standalone/` workspace.

## Verify

- `npm install -g its-magic@0.1.5`
- `its-magic --target . --mode upgrade --host both`
- Confirm `.its-magic/bin/itsm` exists.
